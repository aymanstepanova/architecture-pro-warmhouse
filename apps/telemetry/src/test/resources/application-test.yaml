spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate.default_schema: telemetry
      hibernate.jdbc.time_zone: UTC
    open-in-view: false
  datasource:
    driver-class-name: org.postgresql.Driver

server:
  port: 0  # random
